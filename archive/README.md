# Archive — older / unused validation prototypes

Moved here for code review clarity. Not used by the main simulation (`server.py` + `index.html`) or Schedule Explorer (`validation_v2/`).

| Folder | What it was |
|--------|-------------|
| `validation_v3/` | Later improve/tuning UI |
| `latest/` | Earlier upload-based validation server |
| `test-stage/` | Staging copy of sim + validate |
| `validate/` | Standalone validate server copy |
| `it_power_validation_tool/` | Packaged validation tool |
| `xx/` | Scratch validation scripts |
